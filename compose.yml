version: "3.9"

services:
  frontend:
    build: .

    container_name: windor-frontend

    ports:
      - "3001:3000"

    environment:
      NODE_ENV: production
      PORT: 3000

      NEXT_PUBLIC_BASE_URL: http://13.59.207.17:3000
      NEXT_PUBLIC_AWS_IMAGE_BASE_URL: https://caz-windor-verifications-images.s3.us-east-2.amazonaws.com

    restart: unless-stopped